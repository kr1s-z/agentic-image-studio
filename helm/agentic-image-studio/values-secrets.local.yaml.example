# Copy to values-secrets.local.yaml (gitignored) and fill in key.
# Deploy with Tilt (`tilt up`) or helm manually with -f values-secrets.local.yaml.

backend:
  env:
    createSecret: true
    replicateApiToken: "r8_..."
    extra:
      MAX_ITERATIONS: "5"
